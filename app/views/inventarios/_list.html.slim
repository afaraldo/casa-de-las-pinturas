table.table.table-bordered.table-hover
  thead
    tr
      th Código
      th Nombre
      th Categoría
      th
      th Cantidad
  tbody
    - inventarios.each do |m|
      tr
        td = m.codigo
        td = m.nombre
        td = m.categoria_nombre
        td = link_to 'Movimientos', mercaderias_historico_path, class: 'btn btn-default btn-sm'
        td.text-right = number_with_precision(m.stock_actual, delimiter: '.')
