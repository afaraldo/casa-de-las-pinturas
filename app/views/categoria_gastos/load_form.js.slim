| var modal = $('#main-modal-sm');
| modal.find('.modal-content').html("#{escape_javascript(render partial: 'form', format: :html, locals: {categoria_gasto: @categoria_gasto})}");
| modal.modal('show');