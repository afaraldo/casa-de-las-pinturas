| var modal = $('#main-modal');
| modal.find('.modal-content').html("#{escape_javascript(render partial: 'form', format: :html, locals: {movimiento_egreso: @movimiento_egreso, moneda: @moneda})}");
| modal.modal('show');
