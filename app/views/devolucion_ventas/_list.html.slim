table.table.table-bordered.table-hover
  thead
    tr
      th Fecha
      th Proveedor
      th Motivo
      th Monto
  tbody
    - devolucion_ventas.each do |devolucion|
      tr.fila-clickeable data-url="#{devolucion_venta_path(devolucion)}"
        td = localize(devolucion.fecha.to_date)
        td = devolucion.persona_nombre
        td = devolucion.motivo
        td.text-right = number_to_currency(devolucion.importe_total)